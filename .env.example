# Android Forensic Tool - Environment Configuration
# Copy this file to .env and configure as needed

# ============================================
# DEBUG CONFIGURATION
# ============================================
# Set to 'true' ONLY during development/troubleshooting
# MUST be 'false' in production to prevent information disclosure
DEBUG_MODE=false

# ============================================
# CELL TOWER GEOLOCATION API KEYS
# ============================================
# Cell tower geolocation provides location inference from cellular data
# These services require API keys from external providers

# Option 1: OpenCellID (Recommended - Free tier available)
# Sign up at: https://opencellid.org/
# Get your API key from the API section after registration
OPENCELLID_API_KEY=

# Option 2: Unwired Labs (Alternative - Free tier available)
# Sign up at: https://unwiredlabs.com/
# Get your API key from the dashboard after registration
UNWIREDLABS_API_KEY=

# Note: If both keys are empty, the system will use fallback mode
# Fallback mode provides limited accuracy based on known tower databases

# ============================================
# SERVER CONFIGURATION
# ============================================
# PHP development server port (default: 8080)
SERVER_PORT=8080

# Python path (adjust if Python is not in system PATH)
PYTHON_PATH=python

# ============================================
# LOG PATHS
# ============================================
# Custom log directory (leave empty to use default: ../logs)
LOGS_PATH=

# ============================================
# USAGE INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your settings
# 3. The application will automatically load .env values
# 4. Never commit .env to version control (it's in .gitignore)
